#!/bin/bash# This is a magic comment that tells the batch systen # how many threads you'll be using. Leave the 'nodes'# setting at one, and just adjust the 'ppn' (processes# per node'):#PBS -l walltime=00:01:00#PBS -l nodes=1:ppn=24# This starts the MPI processes using the 'Hydra' job # manager rather than the older 'MPD' one. Note that# the '-n X' must match the number of threads set in# the PBS line above.cd $PBS_O_WORKDIRtime mpiexec.hydra -f ${PBS_NODEFILE} -n 24 DiffractionMainRun